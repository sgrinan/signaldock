apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: signaldock
  namespace: signaldock
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: local-kubernetes
    kind: SecretStore
  target:
    name: signaldock
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: signaldock-source
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: postgres
  namespace: signaldock
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: local-kubernetes
    kind: SecretStore
  target:
    name: postgres
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: postgres-source