apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-provisioning-dashboards
data:
  dashboards.yml: |-
    apiVersion: 1

    providers:
      - name: SignalDock
        folder: SignalDock
        type: file
        updateIntervalSeconds: 30
        options:
          path: /var/lib/grafana/dashboards

