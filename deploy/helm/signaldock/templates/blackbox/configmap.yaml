apiVersion: v1
kind: ConfigMap
metadata:
  name: blackbox
data:
  blackbox.yml: |-
    modules:
      http_2xx:
        prober: http
        timeout: 5s
        http:
          preferred_ip_protocol: ip4

